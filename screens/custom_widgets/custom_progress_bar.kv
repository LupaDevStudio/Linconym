#:kivy 2.2.1

<CustomProgressBar>:

    # Background
    canvas.before:
        Color:
            rgba: self.secondary_color
        Rectangle:
            pos:self.pos
            size:self.size
        Color:
            rgba: self.primary_color
        Rectangle:
            pos:self.pos
            size:(self.size[0]*self.value, self.size[1])
